// What is the purpose of the array splice method
// splice method remove and add the array element in the arrray its take 3 argument first is index position second is howmany element will be add or remove the third is add and remove element write down 
// its modify the original array and and return the delete array
let array = [1, 2, 3, 6, 5, 4]

console.log(array.splice(3, 1, "a", "b"))
console.log(array.splice(0, 3))