// what is the purpose of the array slice method
let array = [1, 2, 3, 5, 6, 6];
console.log(array.slice(1, 4));
// slice method take two argument start and last position or (index) and not return last position element and return the array

