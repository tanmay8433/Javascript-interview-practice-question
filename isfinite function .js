// what is the purpose of isfinite funtion 
// the isFinite fun used to determine wheather a no is a finiety,legal no.
// it return false if value is +infinit - infinit or NaN. otherwise it returns true.
console.log(isFinite(Infinity));
console.log(isFinite(100));
console.log(isFinite(-Infinity));